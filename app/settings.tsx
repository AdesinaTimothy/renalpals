import React from "react";
import { StyleSheet, Text, View } from "react-native";

const settings = () => {
  return (
    <View>
      <Text>This is the settings page now</Text>
    </View>
  );
};

export default settings;

const styles = StyleSheet.create({});
